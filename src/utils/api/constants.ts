export const DEFAULT_BASE_URL = 'https://jsonplaceholder.typicode.com';
export const DEFAULT_MOCK_CONFIG = {
  mockDelay: 2000,
  mockResponse: {
    status: 200,
    data: 'createAPIAction function: mockConfig default data',
  },
};
export const IS_MOCKED = true;