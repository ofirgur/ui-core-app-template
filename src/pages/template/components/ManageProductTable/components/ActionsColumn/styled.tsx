import styled from '@emotion/styled';
import Link from '@mui/material/Link';

export const StyledActionsColumn = styled.ul`
  list-style-type: none;
  display: flex;
  gap: 20px;
`;

export const StyledLink = styled(Link)`
  cursor: pointer;
`;
